import React from 'react';

const CreatePackage = () => {
    return (
        <div style={style.container}>
            <h1>Pantalla de Creacion de Paquete</h1>
        </div>
    );
};

const style = {
    container: {
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center'
    }
}

export default CreatePackage;